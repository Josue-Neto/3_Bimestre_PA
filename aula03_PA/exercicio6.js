// Escreva um programa JavaScript para receber 2 números como entrada. Através da estrutura condicional, exibir os 2 números inseridos somente se a soma dos dois números for menor que 20.

let num1 = 13;
let num2 = 5;
let soma = (num1 + num2);

if (soma <= 20){
    console.log(`${soma} = ${num1} + ${num2}.`);
}